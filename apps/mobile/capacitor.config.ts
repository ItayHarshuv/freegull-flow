import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.freegull.flow',
  appName: 'Freegull Flow',
  webDir: '../web/dist',
};

export default config;
